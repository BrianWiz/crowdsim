# Running
Due to the number of units, it's recommended to run this in release mode for a demonstration.
- First run the server: `cargo run --release -- server`
- Optionally connect a client: `cargo run --release -- client`
